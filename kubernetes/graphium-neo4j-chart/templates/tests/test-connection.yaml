apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "graphium-neo4j.fullname" . }}-test-connection"
  labels:
    {{- include "graphium-neo4j.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "graphium-neo4j.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
